"use strict";
var menssageError = document.getElementById('msg-error');
var formulario = document.getElementById('login');
formulario === null || formulario === void 0 ? void 0 : formulario.addEventListener('submit', function (e) {
    e.preventDefault();
});
